# Build artifacts. The place file is produced by `rojo build`; never commit it,
# and never commit a place saved out of Studio (it would carry generated geometry).
*.rbxlx
*.rbxl
*.rbxlx.lock
*.rbxl.lock

# Regenerated by `rojo sourcemap`; machine-specific and always stale in review.
sourcemap.json
